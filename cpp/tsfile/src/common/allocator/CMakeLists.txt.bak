
message("Running in src/common/allocator directory")

message("CMAKE_CURRENT_SOURCE_DIR=${CMAKE_CURRENT_SOURCE_DIR}")

aux_source_directory(${CMAKE_CURRENT_SOURCE_DIR} common_allocator_SRC_LIST)
set(common_allocator_INC_LIST)
set(common_allocator_DEFINE_OPTIONS)
set(common_allocator_COMPILE_OPTIONS)
set(common_allocator_LINK_OPTIONS)

add_statis_objtarget(common_allocator common_allocator_SRC_LIST common_allocator_INC_LIST
                     "${common_allocator_DEFINE_OPTIONS}"
                     "${common_allocator_COMPILE_OPTIONS}"
                     "${common_allocator_LINK_OPTIONS}")

